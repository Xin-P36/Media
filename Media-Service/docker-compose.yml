services:
  media-service:
    build: .
    container_name: media-service
    ports:
      - "${SERVER_PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      SERVER_PORT: 8080
      MEDIA_SYSTEM: linux
    volumes:
      - ./data:/data
    restart: unless-stopped
